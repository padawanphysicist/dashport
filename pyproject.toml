[project]
name = "foo"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "docopt>=0.6.2",
    "python-decouple>=3.8",
    "requests>=2.32.5",
]

[project.scripts]
dashport = "dashport.__main__:main"
